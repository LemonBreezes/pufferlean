# Per-env overrides for `squared` (layered over config/default.ini, under CLI flags).
# Demonstrates the PufferLib config precedence: these win over default.ini, lose to flags.

[train]
learning_rate = 0.05

[policy]
hidden_size = 24
